{% extends 'base.html.twig' %}

{% block title %}Hello ConsultationController!{% endblock %}

{% block body %}

    <h1>{{ title }}</h1>


    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

</table>

<a href="{{ path('consultations') }}"><button type="button">Retour</button></a>

{% endblock %}
